import { StyleSheet } from "react-native";

export default StyleSheet.create({
    list: { backgroundColor: "#f9f9f9" },
    loadingText: {
        textAlign: 'center',
        fontSize: 18,
        color: '#888', // soft grey for chill loading vibe
        marginTop: 40,
        fontStyle: 'italic', // like a whisper from a tiny bird 🕊️
    },
});